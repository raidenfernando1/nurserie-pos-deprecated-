import { useProducts } from "@/hooks/useProducts";
import useWarehouse from "../../_lib/useWarehouse";

const AddExistingProduct = () => {
  return <h1 className="text-9xl">test</h1>;
};

export default AddExistingProduct;
